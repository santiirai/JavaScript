// Create an array of numbers
const numbers = [12, 7, 9, 14, 23, 36, 45, 56, 67, 78];

// Use forEach() to print all even numbers
numbers.forEach(number => {
    if (number % 2 === 0) {
        console.log(number);
    }
});
